NODE_ENV=production

DB_HOST=localhost
DB_PORT=5432
DB_PASSWORD=CHANGE-ME!
DB_USERNAME=stinky-socks
DB_NAME=stinky-socks

BITBUCKET_API_URL=https://your.bitbucket.server/rest/api/1.0
BITBUCKET_API_TOKEN=your_bitbucket_token

GITHUB_API_TOKEN=your_github_token

USER_NAME_SUFFIX_TO_REPLACE=(optional) part of user logins to replace in the imported data
USER_NAME_PREFIX_TO_REPLACE=(optional) part of user logins to replace in the imported data